#repeats 3 times
for i in range(3):
# asks for categorys and things in that category
    category = input("Enter a category: ")
    first = input("enter something in that category:")
    second = input("enter something in that category:")
    third = input("enter something in that category:")
# prints all responses
    print(category +":" +" "+ first +" "+ second +" "+ third)
